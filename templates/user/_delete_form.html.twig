<div class="content-card">
    <div class="danger-zone">
        <h3 class="section-title" style="color: var(--danger-btn);">Danger Zone</h3>
        <p style="margin-bottom: 1rem;">Once you delete this user, there is no going back. Please be certain.</p>
        
        <form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this user?');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
            <button class="btn btn-danger">
                <i class="fas fa-trash btn-icon"></i> Delete User
            </button>
        </form>
    </div>
</div>